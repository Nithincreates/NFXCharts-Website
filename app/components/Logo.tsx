export default function Logo() {
  return (
    <span className="inline-flex items-center gap-3">
      <span className="text-xs font-semibold tracking-[0.14em] text-white sm:text-sm sm:tracking-[0.18em]">
        NFXCHARTS
      </span>
    </span>
  );
}
